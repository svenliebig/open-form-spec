{
  "name": "@ofs/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@ofs/types": "*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "@types/node": "^22.0.0"
  }
}